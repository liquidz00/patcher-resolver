[project]
name = "patcher-resolver"
version = "0.1.0"
description = "Mac Mini macOS Installomator label resolver for Patcher (Temporal, shadow mode)."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "temporalio>=1.28.0",
    "httpx>=0.27",
    "pydantic-settings>=2.5",
]
